name: Deploy Docs

on:
  push:
    branches: [feat/gh-pages-restyles]
  workflow_dispatch:

jobs:
  docs:
    uses: anishhs-gh/restyles/.github/workflows/deploy.yml@main
    permissions:
      contents: read
      pages: write
      id-token: write
